# agentmoat: expected verdict = incompatible (raw-socket)
apiVersion: v1
kind: Pod
metadata:
  name: ping-tool
  namespace: default
spec:
  containers:
    - name: pinger
      image: busybox:1.36
      command: ["sleep", "3600"]
      securityContext:
        capabilities:
          add: ["NET_RAW"]
