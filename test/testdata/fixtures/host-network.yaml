# agentmoat: expected verdict = incompatible (host-network)
apiVersion: v1
kind: Pod
metadata:
  name: host-net-app
  namespace: default
spec:
  hostNetwork: true
  containers:
    - name: app
      image: busybox:1.36
      command: ["sleep", "3600"]
