# agentmoat: expected verdict = incompatible (privileged)
apiVersion: v1
kind: Pod
metadata:
  name: privileged-app
  namespace: default
spec:
  containers:
    - name: app
      image: busybox:1.36
      command: ["sleep", "3600"]
      securityContext:
        privileged: true
